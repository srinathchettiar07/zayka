<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
<div class="flex min-h-screen">
    <%- include('partials/dashboard.ejs') %>
    <div class="flex-1 p-6 bg-white text-black">
        <nav> 
            <div class="flex items-center w-[600px] h-12 bg-white border border-gray-300 rounded-full shadow-sm px-4">
                <!-- Location Icon & Text -->
                <div class="flex items-center space-x-2 text-gray-500">
                    <i class="fa-solid fa-location-dot text-red-500"></i>
                    <select name="" id="" class="text-xl">
                        <option value="sri">sri</option>
                    </select>
                </div>
            
                <!-- Divider -->
                <div class="w-px h-6 bg-gray-300 mx-4"></div>
            
                <!-- Search Input -->
                <div class="flex items-center text-gray-400 w-full">
                    <i class="fa-solid fa-magnifying-glass"></i>
                    <form action="/user/dashboard" method="post">
                        <input type="text" placeholder="Search for dish" class="w-full outline-none px-2 text-gray-600" name="name">
                    </form>
                    
                </div>
            </div>
        </nav>
        <main>
            <div class="text-3xl md:text-4xl font-bold mb-8 md:mb-12 text-center text-gray-800">
                Recipes:
            </div>

            <div>
                <% if(locals.foods){ %>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 w-11/12 max-w-6xl">
                        <% for(let i = 0; i < Math.min(15, foods.length); i++) { %>
                            <div class="bg-white border-2 border-red-500 rounded-lg p-5">
                                <!-- Recipe Name -->
                                <h2 class="text-2xl font-semibold text-gray-800"><%= foods[i].name %></h2>
                
                                <!-- Recipe Image -->
                                <div class="mt-3">
                                    <img src="<%= foods[i].img_url %>" alt="<%= foods[i].name %>" class="w-full h-48 object-cover rounded-lg">
                                </div>
                
                                <!-- Recipe Details -->
                                <p class="text-gray-700 mt-3"><strong>🥗 Ingredients:</strong> <%= foods[i].ingredients %></p>
                                <p class="text-gray-700"><strong>🥩 Diet:</strong> <span class="text-red-500"><%= foods[i].diet %></span></p>
                                <p class="text-gray-700"><strong>⏳ Prep Time:</strong> <%= foods[i].prep_time %> minutes</p>
                                <p class="text-gray-700"><strong>🔥 Cook Time:</strong> <%= foods[i].cook_time %> minutes</p>
                                <p class="text-gray-700"><strong>🌶️ Flavor:</strong> <%= foods[i].flavor_profile %></p>
                                <p class="text-gray-700"><strong>🍽️ Course:</strong> <%= foods[i].course %></p>
                                <p class="text-gray-700"><strong>📍 State:</strong> <%= foods[i].state %></p>
                                <p class="text-gray-700"><strong>🌍 Region:</strong> <%= foods[i].region %></p>
                            </div>
                        <% } %>
                    </div>
                <% }else { %>
                  <div class="text-red-500">
                    <h1>No recipes found. Try another name</h1>
                  </div>
                <% } %>
            </div>
        </main>

    </div>
    
    
</div>
</body>
</html>